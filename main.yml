- hosts: localhost
  connection: local
  gather_facts: no
  become: yes
  become_user: "{{ user }}"
  roles:
    - homebrew
    - homebrew-cask
    - ricty-font
    - vim
    - anyenv

  vars:
    user: "{{ lookup('env', 'USER') }}"
